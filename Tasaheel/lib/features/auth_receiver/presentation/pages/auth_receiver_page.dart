import 'package:flutter/material.dart';

class Auth_receiverPage extends StatelessWidget {
  const Auth_receiverPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Auth_receiver'),
      ),
      body: Center(
        child: Text('Welcome to auth_receiver page!'),
      ),
    );
  }
}
